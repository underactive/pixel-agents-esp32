# Version History

| Ver | Changes |
|-----|---------|
| 0.1.0 | Initial implementation: sprite converter, ESP32 firmware (renderer, FSM, BFS pathfinding, serial protocol), Python companion bridge, project docs |
| 0.2.0 | Office Tileset integration: extract tiles from PNG spritesheet for floor/wall/furniture rendering, tileset support in layout editor, fallback to hand-drawn sprites when tileset absent |
| 0.3.0 | Always-visible characters: all 6 characters idle in social zones (break room, library) at boot, walk to desks when agents activate, walk back when inactive; status bar shows "N/6 active" |
| 0.4.0 | French Bulldog pet: 16x16 animated dog roams the office with WANDER/FOLLOW/NAP behavior FSM, depth-sorted with characters, BFS pathfinding, sprite generator tool |
| 0.4.1 | Updated dog sprites: replaced hand-drawn 32x24 with 25x19 pixel art from PNG sprite sheet, 23 frames (8 idle, 4 walk, 8 run, sit, lay down, pee), side-view only with LEFT flip, added run and pee behaviors |
| 0.5.0 | Hamburger menu + multi-color dog: 4 dog color variants (black, brown, gray, tan), CYD touch hamburger menu for dog toggle and color selection, NVS settings persistence across reboots |
| 0.6.0 | Screenshot capture: press 's' in companion to capture ESP32 framebuffer as BMP/PNG, RLE-compressed transfer over serial, LILYGO full-buffer only (CYD returns graceful error) |
| 0.6.1 | CYD screenshot support: read pixels back from ILI9341 display controller via SPI readRect row-by-row, no framebuffer needed |
| 0.7.0 | Animated boot splash screen: random character at 2x scale with walk-down animation, verbose boot log, backlight fade transition to office scene on companion connection |
| 0.7.1 | Splash screenshot capture: re-render splash into sprite buffer for screenshot during boot, CYD half-buffer two-pass support, footer with version string, audit fixes (null guard, _drawYOffset consistency) |
| 0.8.0 | BLE transport + PIN pairing: NimBLE Nordic UART Service for untethered operation, Transport abstraction layer, lock-free SPSC ring buffer with atomic memory ordering, companion --transport ble mode with bleak, 4-digit PIN per boot for multi-CYD device selection via manufacturer advertising data, --ble-pin CLI option, interactive PIN prompt, unified session-state reset on reconnect |
| 0.8.1 | Companion launcher script: `run_companion.py` auto-creates venv, installs deps (hash-cached), forwards CLI args — replaces 5-step manual setup with single command |
| 0.8.2 | Browser firmware flasher + CI/CD: standalone `tools/firmware_update.html` using Web Serial API + esptool-js for flashing CYD/LILYGO from Chrome, drag-and-drop file selection, two flash modes (update only / full flash), GitHub Actions workflow for automated firmware builds and releases on tag push |
| 0.8.3 | ESP32 thermal management: junction temperature monitoring with thermal soak timing, thermal throttling with backlight management and CYD fault indicator LED |
| 0.8.4 | CYD display improvements: flip screen toggle in hamburger menu, strip-buffer fallback replacing half-screen buffer for no-PSRAM rendering |
| 0.8.5 | Native macOS companion menu bar app (Swift/SwiftUI, serial + BLE transport, usage stats API), BLE reconnection fix with explicit Connect button, spawn effect duration increase to 3s, README corrections, future improvements housekeeping |
| 0.8.6 | Fix CI release build (pioarduino platform URL for Arduino Core 3.x), fix usage stats fetch-on-launch and reduce poll to 15min |
| 0.8.7 | Custom pixel art app icon + menu bar silhouette for macOS companion, BLE UX improvements (auto-scan, colored connect/disconnect buttons, preserve devices after manual disconnect), fix keepDevicesOnCleanup flag leak, fix zombie CoreBluetooth connections on discovery failure |
| 0.8.8 | Fix agent IDs >= 6 silently rejected by firmware, add agent ID recycling in companions to keep IDs low across agent lifecycle |
| 0.9.1 | Add Freenove ESP32-S3 (`freenove-s3-28c`) build and artifact packaging to GitHub Actions release workflow — releases now include 3 zip files (CYD, LILYGO, Freenove S3) |
| 0.9.2 | Add ESP-SR WakeNet9 wake word detection on CYD-S3 ("Computer" trigger → DOG_BARK), dedicated FreeRTOS task on Core 0, custom partition table for ESP-SR model storage, remove mic loopback test, audit fixes (atomic sync, validation guards, named constants) |
| 0.9.3 | Add "Remaining" usage display mode in macOS companion — tap "Usage" header to toggle between used and remaining percentage, persisted via @AppStorage, color thresholds always based on used percentage |
| 0.9.4 | Add display sleep mode with touch-to-wake, battery monitor (CYD-S3/LILYGO) with color-coded status bar percentage, transport connection icons (USB/BT) in status bar, BLE Battery Service for native macOS battery display, larger hamburger menu, OAuth token caching in macOS companion, serial connect/disconnect buttons in macOS companion |
| 0.9.6 | Fix macOS companion high CPU usage: replace 4Hz blind polling with FSEvents-driven transcript processing, guard displayAgents with equality check, directory-level coalesced events |
| 0.9.5 | Add macOS companion app code signing, notarization, and DMG packaging to release workflow; parallel firmware+macOS build jobs; `make sign` target for local Developer ID builds |
| 0.10.0 | Add Software Display mode to macOS companion: local 320x224 pixel-art office rendering (full character FSM, BFS pathfinding, dog AI), PIP floating window with NSPanel (nearest-neighbor scaling, aspect-ratio lock, hover titlebar), Display Mode picker (ESP32 Device / Software), OfficeRenderer producing shared CGImage at 15 FPS, office background PNG generator tool, rename Serial→USB / BLE→Bluetooth labels, dark mode icon fix, `make sign-install` target, macOS 14.0 deployment target. Add Settings window (gear button in popover) with Launch at Login, Claude/Codex usage stats toggles, auto-update toggle. Add right-click context menu on menu bar icon with About, Check for Updates, and Quit. Add Sparkle auto-update framework with EdDSA-signed appcast on GitHub Pages and release workflow integration. |
| 0.10.1 | Add custom DMG background with styled icon layout using create-dmg, fix appcast EdDSA signature extraction and version deduplication |
| 0.10.2 | Add Ko-fi donation link to macOS companion About window |
| 0.10.3 | Fix Sparkle auto-update not offering downloads (CFBundleVersion mismatch), add Ko-fi donation link to About window |
| 0.10.4 | Move "Check for Updates" from right-click menu to About window with last-checked timestamp, standardize Ko-fi button style |
| 0.10.5 | Add remote device settings over serial/BLE: new DEVICE_SETTINGS (0x07) and SETTINGS_STATE (0x08) protocol messages, Device tab in Settings window with dog/color/flip/sound controls, BLE TX send for device-to-companion messages, bidirectional settings sync with device as source of truth |
| 0.10.6 | Add Cursor IDE support to macOS companion: usage stats via api2.cursor.sh (Bearer auth from vscdb token), agent detection from ~/.cursor/projects/ JSONL transcripts, settings toggle. Replace SF Symbol icons with brand SVG logos (Claude, Codex, Cursor) via reusable SVGPathShape renderer. Include Gemini icon for future use. |
| 0.10.7 | Add Google Gemini CLI support: agent activity detection from ~/.gemini/tmp/ session JSON, usage stats via Google Cloud quota API (OAuth), pink #C48BB0 brand color, GeminiStateDeriver for monolithic JSON sessions, GeminiUsageFetcher with token refresh + project discovery, Python companion Gemini watching, fix Xcode test host for spaced PRODUCT_NAME. |
| 0.11.0 | Add activity heatmaps for Claude, Codex, and Gemini: local SQLite database records daily tool call counts, GitHub-style 53-week contribution grids with per-provider brand color palettes (orange/blue/pink), shared HeatmapGridView extracted from Cursor heatmap, Cursor heatmap unchanged (API-driven green). |
| 0.11.1 | Add iCloud Drive sync for activity heatmap data across Macs (ubiquitous container), iCloud sync settings toggle with entitlement, fix Gemini usage stuck at 100% by filtering expired quota buckets whose resetTime is in the past. |
| 0.11.2 | Fix Cursor usage stuck at "Loading..." in macOS companion when Cursor's SQLite WAL file is absent — use immutable URI mode to skip WAL/SHM handling. |
| 0.11.3 | Add device fingerprinting (MSG_IDENTIFY_REQ/RSP 0x09/0x0A) for companion to verify Pixel Agents device identity, BLE NUS TX notification support in Python companion, fix iCloud activity sync device ID using hardware UUID. |
| 0.11.4 | Fix Settings window tabs not visible (increase window height), fix Device settings tab stuck on "Waiting for device settings..." (wrap serial/BLE transport callbacks in Task { @MainActor in } for correct actor isolation). |
| 0.9.0 | Add OpenAI Codex CLI support to both companions — watch `~/.codex/sessions/` rollout JSONL alongside Claude Code transcripts, each source gets its own agent characters, no firmware changes needed. Fix Codex state derivation for current snake_case JSONL format (`response_item`/`event_msg`); three-format support (current, `codex exec --json`, legacy PascalCase). Add ESP32-S3 CYD board support (`freenove-s3-28c`) with ILI9341 + FT6336G capacitive touch + BLE + WS2812B NeoPixel LED (GPIO 42), unified LED ambient system across CYD (PWM) and CYD-S3 (NeoPixel), fix TFT_eSPI SPI register address bug on ESP32-S3 (`USE_FSPI_PORT`), fix color inversion on S3 CYD panel. Add event-driven sound system on CYD-S3 (I2S + ES8311 codec) and CYD (ESP32 internal DAC on GPIO 26 → SC8002B amp): table-driven SoundId enum with 5 clips (startup chime, dog bark, keyboard typing, notification click, permission pop), CYD uses `huge_app.csv` partition (3MB app, no OTA) to fit PCM data, `tools/convert_sound.py` for MP3→PCM header conversion, permission bubble detection via timeout heuristic in Python companion. Sound on/off toggle in hamburger menu (CYD defaults off, CYD-S3 defaults on), persisted to NVS. Simplify agent list UI: black source icons, hide icon for offline agents |
